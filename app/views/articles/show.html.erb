<h1><%= @article.title %></h1>

<h3><%= @article.content %></h3>

<%= link_to 'edit', edit_article_path %>

<%= link_to "delete",
            article_path(@article),
            method: :delete,
            data: { confirm: 'Are you sure?' }  %>
